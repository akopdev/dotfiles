#!/bin/zsh

docker ps --format '{{.Names}}' | 

fzf --ansi --no-sort --reverse --tiebreak=index --bind=ctrl-s:toggle-sort \
    --bind "ctrl-m:execute(clear && docker exec -i -t {} sh)" \
    --header "Exec shell" \
    --preview "(docker logs {} --tail 50 | less -R +G)" 
