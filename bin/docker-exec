#!/bin/zsh

clear

docker ps --format '{{.Names}}' \
| fzf --ansi --no-sort --reverse --tiebreak=index --bind=ctrl-s:toggle-sort \
    --header "Exec shell" \
    --preview "(docker logs {} --tail 100 | less -R +G)" \
| xargs -I % -o docker exec -i -t % sh
